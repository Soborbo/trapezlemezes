---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Sikeres megrendeles | Trapezlemezes.hu" noindex hideNotifications>
  <div class="success-page">
    <div class="success-card">
      <div class="success-icon">
        <svg width="64" height="64" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="12" cy="12" r="10" fill="#16a34a"/>
          <path d="M8 12l2.5 2.5L16 9" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <h1>Megrendelesi szandekot rogzitettuk!</h1>

      <div class="order-info" id="order-info">
        <p>Megrendeles azonosito: <strong id="order-id"></strong></p>
      </div>

      <div class="next-steps">
        <h2>Mi tortenik most?</h2>
        <ol>
          <li>
            <span class="step-number">1</span>
            <div>
              <strong>Email visszaigazolast kuldtunk</strong>
              <p>Nehany percen belul megkapja a megrendelesi osszesitot e-mailben.</p>
            </div>
          </li>
          <li>
            <span class="step-number">2</span>
            <div>
              <strong>24 oran belul felhivjuk es veglegesitjuk a rendelest</strong>
              <p>Munkatarsunk felveszi Onnel a kapcsolatot, egyeztetjuk a reszleteket.</p>
            </div>
          </li>
          <li>
            <span class="step-number">3</span>
            <div>
              <strong>Gyartas es szallitas az egyeztetett hataridore</strong>
              <p>A veglegesites utan 3-5 munkanapon belul legyartjuk es kiszallitjuk.</p>
            </div>
          </li>
        </ol>
      </div>

      <div class="reminder-notice">
        <p>Ez szandeknyilatkozat -- nem vegleges megrendeles. A vegleges rendeles csak telefonos egyeztetes utan jon letre.</p>
      </div>

      <div class="contact-info">
        <p>Kerdese van? Hivjon minket:</p>
        <a href="tel:+3613009206" class="phone-link">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          +36 1 300-92-06
        </a>
      </div>

      <a href="/" class="btn btn-secondary">Vissza a fooldalra</a>
    </div>
  </div>
</Layout>

<style>
  .success-page {
    min-height: calc(100vh - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-xl) var(--spacing-md);
    background: var(--color-bg-light);
  }

  .success-card {
    background: var(--color-white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    padding: var(--spacing-2xl);
    max-width: 600px;
    width: 100%;
    text-align: center;
  }

  .success-icon {
    margin-bottom: var(--spacing-lg);
  }

  h1 {
    color: var(--color-text);
    font-size: var(--font-size-2xl);
    margin: 0 0 var(--spacing-md) 0;
  }

  /* Order info */
  .order-info {
    background: #f0fdf4;
    border: 1px solid #bbf7d0;
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
  }

  .order-info p {
    margin: 0;
    color: #166534;
  }

  .order-info strong {
    font-family: monospace;
    font-size: var(--font-size-lg);
  }

  /* Next steps */
  .next-steps {
    text-align: left;
    margin-bottom: var(--spacing-xl);
  }

  .next-steps h2 {
    font-size: var(--font-size-lg);
    color: var(--color-text);
    margin: 0 0 var(--spacing-lg) 0;
    text-align: center;
  }

  .next-steps ol {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .next-steps li {
    display: flex;
    gap: var(--spacing-md);
    padding: var(--spacing-md) 0;
    border-bottom: 1px solid var(--color-bg-light);
  }

  .next-steps li:last-child {
    border-bottom: none;
  }

  .step-number {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #259bd7;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    flex-shrink: 0;
  }

  .next-steps strong {
    display: block;
    color: var(--color-text);
    margin-bottom: 4px;
  }

  .next-steps p {
    margin: 0;
    color: var(--color-text-light);
    font-size: var(--font-size-sm);
  }

  /* Reminder notice */
  .reminder-notice {
    background: linear-gradient(135deg, #e3f2fd 0%, #e8eaf6 100%);
    border-left: 4px solid var(--color-secondary);
    border-radius: 0 var(--radius-md) var(--radius-md) 0;
    padding: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
    text-align: left;
  }

  .reminder-notice p {
    margin: 0;
    font-size: var(--font-size-sm);
    color: var(--color-text);
    font-style: italic;
    line-height: 1.5;
  }

  /* Contact info */
  .contact-info {
    background: var(--color-bg-light);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
  }

  .contact-info p {
    margin: 0 0 var(--spacing-sm) 0;
    color: var(--color-text-light);
    font-size: var(--font-size-sm);
  }

  .phone-link {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-sm);
    color: #259bd7;
    font-size: var(--font-size-xl);
    font-weight: 600;
    text-decoration: none;
  }

  .phone-link:hover {
    text-decoration: underline;
  }

  /* Button */
  .btn-secondary {
    display: inline-block;
    padding: var(--spacing-sm) var(--spacing-lg);
    background: var(--color-bg-light);
    color: var(--color-text);
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 500;
    transition: background 0.2s;
  }

  .btn-secondary:hover {
    background: var(--color-bg-dark);
    color: var(--color-text);
  }
</style>

<script>
  // Get order ID from URL
  const params = new URLSearchParams(window.location.search);
  const orderId = params.get('id');

  if (orderId) {
    const orderIdEl = document.getElementById('order-id');
    if (orderIdEl) {
      orderIdEl.textContent = orderId;
    }
  } else {
    const orderInfo = document.getElementById('order-info');
    if (orderInfo) {
      orderInfo.style.display = 'none';
    }
  }

  // GTM Conversion Event - Order Submitted
  if (typeof window !== 'undefined' && window.dataLayer) {
    window.dataLayer.push({
      event: 'order_completed',
      conversion_type: 'order_submission',
      order_id: orderId || undefined,
      currency: 'HUF',
    });
  }
</script>
