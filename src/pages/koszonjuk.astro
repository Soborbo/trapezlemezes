---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Köszönjük az ajánlatkérést - Trapezlemezes.hu" noindex hideNotifications>
  <div class="thank-you-page">
    <div class="thank-you-card">
      <div class="success-icon">
        <svg width="64" height="64" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
          <circle cx="12" cy="12" r="10" fill="#16a34a"/>
          <path d="M8 12l2.5 2.5L16 9" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>

      <h1>Köszönjük az ajánlatkérést!</h1>

      <p class="lead">
        Az árajánlatot elküldtük a megadott e-mail címre.
      </p>

      <div class="quote-info" id="quote-info">
        <p>Ajánlat azonosító: <strong id="quote-id"></strong></p>
      </div>

      <div class="next-steps">
        <h2>Mi történik most?</h2>
        <ol>
          <li>
            <span class="step-number">1</span>
            <div>
              <strong>E-mail megerősítés</strong>
              <p>Néhány percen belül megkapja az árajánlatot e-mailben.</p>
            </div>
          </li>
          <li>
            <span class="step-number">2</span>
            <div>
              <strong>Telefonos egyeztetés</strong>
              <p>Munkatársunk 24 órán belül felveszi Önnel a kapcsolatot.</p>
            </div>
          </li>
          <li>
            <span class="step-number">3</span>
            <div>
              <strong>Gyártás és szállítás</strong>
              <p>Megrendelés után 3-5 munkanapon belül legyártjuk és kiszállítjuk.</p>
            </div>
          </li>
        </ol>
      </div>

      <div class="contact-info">
        <p>Kérdése van? Hívjon minket:</p>
        <a href="tel:+36309386050" class="phone-link">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
          +36 30 938 6050
        </a>
      </div>

      <a href="/" class="btn btn-secondary">Vissza a főoldalra</a>
    </div>
  </div>
</Layout>

<style>
  .thank-you-page {
    min-height: calc(100vh - 200px);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--spacing-xl) var(--spacing-md);
    background: var(--color-bg-light);
  }

  .thank-you-card {
    background: var(--color-white);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    padding: var(--spacing-2xl);
    max-width: 600px;
    text-align: center;
  }

  .success-icon {
    margin-bottom: var(--spacing-lg);
  }

  h1 {
    color: var(--color-text);
    font-size: var(--font-size-2xl);
    margin: 0 0 var(--spacing-md) 0;
  }

  .lead {
    color: var(--color-text-light);
    font-size: var(--font-size-lg);
    margin: 0 0 var(--spacing-lg) 0;
  }

  .quote-info {
    background: #f0fdf4;
    border: 1px solid #bbf7d0;
    border-radius: var(--radius-md);
    padding: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
  }

  .quote-info p {
    margin: 0;
    color: #166534;
  }

  .quote-info strong {
    font-family: monospace;
    font-size: var(--font-size-lg);
  }

  .next-steps {
    text-align: left;
    margin-bottom: var(--spacing-xl);
  }

  .next-steps h2 {
    font-size: var(--font-size-lg);
    color: var(--color-text);
    margin: 0 0 var(--spacing-lg) 0;
    text-align: center;
  }

  .next-steps ol {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .next-steps li {
    display: flex;
    gap: var(--spacing-md);
    padding: var(--spacing-md) 0;
    border-bottom: 1px solid var(--color-bg-light);
  }

  .next-steps li:last-child {
    border-bottom: none;
  }

  .step-number {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #259bd7;
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 600;
    flex-shrink: 0;
  }

  .next-steps strong {
    display: block;
    color: var(--color-text);
    margin-bottom: 4px;
  }

  .next-steps p {
    margin: 0;
    color: var(--color-text-light);
    font-size: var(--font-size-sm);
  }

  .contact-info {
    background: var(--color-bg-light);
    border-radius: var(--radius-md);
    padding: var(--spacing-lg);
    margin-bottom: var(--spacing-lg);
  }

  .contact-info p {
    margin: 0 0 var(--spacing-sm) 0;
    color: var(--color-text-light);
    font-size: var(--font-size-sm);
  }

  .phone-link {
    display: inline-flex;
    align-items: center;
    gap: var(--spacing-sm);
    color: #259bd7;
    font-size: var(--font-size-xl);
    font-weight: 600;
    text-decoration: none;
  }

  .phone-link:hover {
    text-decoration: underline;
  }

  .btn-secondary {
    display: inline-block;
    padding: var(--spacing-sm) var(--spacing-lg);
    background: var(--color-bg-light);
    color: var(--color-text);
    border-radius: var(--radius-md);
    text-decoration: none;
    font-weight: 500;
    transition: background 0.2s;
  }

  .btn-secondary:hover {
    background: var(--color-bg-dark);
  }
</style>

<script>
  // Get quote ID from URL
  const params = new URLSearchParams(window.location.search);
  const quoteId = params.get('quote');

  if (quoteId) {
    const quoteIdEl = document.getElementById('quote-id');
    if (quoteIdEl) {
      quoteIdEl.textContent = quoteId;
    }
  } else {
    const quoteInfo = document.getElementById('quote-info');
    if (quoteInfo) {
      quoteInfo.style.display = 'none';
    }
  }
</script>
